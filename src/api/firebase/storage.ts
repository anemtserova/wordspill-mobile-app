export const getImages = () => {
	return [];
};

export const uploadImage = (imageUri: string) => {
	return 'https://example.com/image.jpg';
};

export const deleteImage = (imageUrl: string) => {
	return true;
};
